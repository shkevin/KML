#include "activation.h"

namespace NeuralNetwork {

    Activation::Activation(Activators a_activation) {

    }
    const double Activation::forward(double input) {
        return 0.0;
    }
    const double Activation::backward(double input) {
        return 0.0;
    }
}

#include "cluster.h"

namespace Clustering {

    // Constructors
    /* Cluster(vector<Point> points, int cluster_id) : points(points), cluster_id(cluster_id) {} */
    /* Cluster(int cluster_id) : cluster_id(cluster_id) {} */

    /* //Functions */
    /* Point center(Cluster c, string type="centroid") { */
    /*     if (type == "centroid") { */
    /*         return this->centroid(c); */
    /*     } */
    /*     return Point(0, 0); */
    /* } */

    /* Point centroid(Cluster c) { */
    /*     double x, y; */
    /*     double sum_x, sum_y; */

    /*     for (int i = 0; i < c.num_points; i++) { */
    /*         sum_x += c.points[i].x; */
    /*         sum_y += c.points[i].y; */
    /*     } */

    /*     x = sum_x / c.num_points; */
    /*     y = sum_x / c.num_points; */

    /*     return Point(x, y); */
    /* } */
}

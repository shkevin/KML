#include "kmeans.h"

namespace Unsupervised {

    namespace Clustering {

        /* Kmeans::Kmeans(int k, int max_iter, float tol) : k(k), max_iter(max_iter), tol(tol) { */
        /*     for(int i; i < k; i++) { */
        /*         this->cluster_ids.push_back(i); */
        /*     } */
        /* } */

        /* Kmeans::~Kmeans() {} */

        /* void Kmeans::fit(vector<vector<int>> X) { */

        /* } */

        /* vector<int> Kmeans::predict(vector<vector<int>> X) { */

        /* } */
    }
}
